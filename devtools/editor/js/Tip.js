var Tip = (function () {

    var tpl = [
        '<div class="tip-item arrow_box">',
        '</div>'
    ].join(",");

    var generate = function (html) {

    }

    return {
        generate: generate
    }
})()